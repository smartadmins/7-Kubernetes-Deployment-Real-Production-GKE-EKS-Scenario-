apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: production

spec:
  storageClassName: local-path

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 20Gi